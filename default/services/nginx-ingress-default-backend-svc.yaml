apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: nginx-ingress
  name: nginx-ingress-default-backend
spec:
  clusterIP: <unknown>
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: nginx-ingress
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
